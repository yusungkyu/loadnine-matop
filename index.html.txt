<!DOCTYPE html>
<html lang="ko">
<head>
<link rel="manifest" href="/manifest.json">
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>검은 마녀의 탑 실시간 위치</title>
<style>
* { box-sizing: border-box; }
body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: #f3f4f6;
    color: #111827;
    font-size: 14px;
}
.page {
    max-width: 1000px;
    margin: 0 auto;
    padding: 10px;
}
.page-title {
    font-size: 26px;
    font-weight: bold;
    text-align: center;
    margin-bottom: 18px;
}
.board-controls {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    margin-bottom: 10px;
}
.tabs {
    display: flex;
    gap: 6px;
    margin-bottom: 0;
}
.tab-btn {
    border: none;
    border-radius: 8px;
    padding: 9px 14px;
    background: #d1d5db;
    color: #111827;
    font-size: 14px;
    cursor: pointer;
}
.tab-btn.active {
    background: #2563eb;
    color: #fff;
}
.refresh-bar {
    display: flex;
    justify-content: flex-end;
    margin-bottom: 0;
    margin-left: auto;
}
.refresh-btn {
    border: none;
    border-radius: 8px;
    padding: 9px 14px;
    background: #2563eb;
    color: #fff;
    font-size: 14px;
    cursor: pointer;
}
.refresh-btn:disabled {
    opacity: 0.7;
    cursor: default;
}
.board-wrap {
    background: #fff;
    border: 1px solid #d1d5db;
    border-radius: 10px;
    overflow: hidden;
}
.image-wrap {
    position: relative;
    width: 100%;
    background: #fff;
    line-height: 0;
}
.image-wrap img {
    display: block;
    width: 100%;
    height: auto;
    user-select: none;
    -webkit-user-drag: none;
}
.pin-layer {
    position: absolute;
    inset: 0;
}
.pin {
    position: absolute;
    width: 18px;
    height: 18px;
    transform: translate(-50%, -50%);
    border: 2px solid #fff;
    border-radius: 50%;
    background: #dc2626;
    box-shadow: 0 2px 6px rgba(0,0,0,0.28);
    cursor: pointer;
    z-index: 2;
}
.pin-label {
    position: absolute;
    transform: translate(-50%, calc(-100% - 16px));
    background: rgba(17,24,39,0.88);
    color: #fff;
    padding: 3px 6px;
    border-radius: 5px;
    font-size: 11px;
    white-space: nowrap;
    pointer-events: none;
    z-index: 1;
}
.section {
    margin-top: 10px;
    background: #fff;
    border: 1px solid #d1d5db;
    border-radius: 10px;
    padding: 10px;
}
.section-title {
    font-size: 14px;
    font-weight: bold;
    margin-bottom: 8px;
}
.pin-list {
    display: flex;
    flex-direction: column;
    gap: 0;
}
.pin-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    padding: 8px 0;
    border-bottom: 1px solid #e5e7eb;
}
.pin-item:last-child {
    border-bottom: none;
}
.pin-item-text {
    flex: 1;
    min-width: 0;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    font-size: 13px;
}
.delete-btn {
    border: none;
    border-radius: 6px;
    background: #dc2626;
    color: #fff;
    padding: 6px 10px;
    cursor: pointer;
    font-size: 12px;
    flex-shrink: 0;
}
.empty-text {
    color: #6b7280;
    font-size: 13px;
    padding: 6px 0;
}
.modal-backdrop {
    position: fixed;
    inset: 0;
    display: none;
    align-items: center;
    justify-content: center;
    background: rgba(0,0,0,0.45);
    z-index: 1000;
    padding: 16px;
}
.modal {
    width: 100%;
    max-width: 340px;
    background: #fff;
    border-radius: 10px;
    padding: 14px;
    box-shadow: 0 10px 24px rgba(0,0,0,0.22);
}
.modal h3 {
    margin: 0 0 10px 0;
    font-size: 16px;
}
.form-row, .view-row {
    margin-bottom: 10px;
}
.form-row,
.view-row {
    display: grid;
    grid-template-columns: 44px 1fr;
    align-items: center;
    gap: 6px;
}
.form-row label, .view-label {
    display: block;
    margin-bottom: 5px;
    font-size: 13px;
    font-weight: bold;
}
.form-row label,
.view-label {
    margin-bottom: 0;
}
.form-row input,
.form-row select {
    width: 100%;
    padding: 10px;
    font-size: 15px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    box-sizing: border-box;
}
.view-value {
    padding: 8px 10px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    background: #f9fafb;
    font-size: 13px;
}
.modal-buttons {
    display: flex;
    justify-content: flex-end;
    gap: 8px;
    margin-top: 12px;
}
.btn {
    border: none;
    border-radius: 6px;
    padding: 8px 12px;
    font-size: 13px;
    cursor: pointer;
}
.btn-gray { background: #d1d5db; color: #111827; }
.btn-blue { background: #2563eb; color: #fff; }
.btn-red { background: #dc2626; color: #fff; margin-right: auto; }
.status-text {
    margin-top: 8px;
    font-size: 12px;
    color: #6b7280;
}

.intro-modal {
    max-width: 360px;
}
.intro-message {
    font-size: 15px;
    font-weight: bold;
    margin-bottom: 12px;
}
.intro-info-box {
    border: 1px solid #d1d5db;
    border-radius: 8px;
    background: #f9fafb;
    padding: 12px;
    font-size: 14px;
    line-line-height: 1.7;
    margin-bottom: 12px;
}
@media (max-width: 768px) {
    .intro-modal {
        max-width: 340px;
    }
}

.profile-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    margin-bottom: 12px;
    padding: 10px 12px;
    background: #fff;
    border: 1px solid #d1d5db;
    border-radius: 10px;
}
.profile-info {
    display: flex;
    align-items: center;
    gap: 18px;
    min-width: 0;
    flex: 1;
    flex-wrap: wrap;
}
.profile-item {
    min-width: 0;
    font-size: 17px;
    color: #111827;
}
.profile-item strong {
    font-weight: bold;
}
.profile-edit-btn {
    border: none;
    border-radius: 6px;
    background: #2563eb;
    color: #fff;
    padding: 8px 12px;
    cursor: pointer;
    font-size: 13px;
    flex-shrink: 0;
}
@media (max-width: 768px) {
    .page { padding: 8px; }
    .page-title {
        font-size: 20px;
        margin-bottom: 14px;
    }
    .board-controls {
        flex-direction: column;
        align-items: stretch;
        gap: 10px;
    }
    .tabs {
        margin-bottom: 0;
    }
    .tab-btn { flex: 1; }
    .refresh-bar {
        margin-left: 0;
    }
    .refresh-btn { width: 100%; }
    .pin { width: 14px; height: 14px; }
    .pin-label {
        display: block;
        padding: 2px 4px;
        font-size: 9px;
        border-radius: 4px;
        transform: translate(-50%, calc(-100% - 10px));
    }
    .pin-item { align-items: flex-start; }
    .modal-buttons { flex-direction: column; }
    .btn-red { margin-right: 0; }
    .btn { width: 100%; }
    .form-row {
        grid-template-columns: 44px 1fr;
    }
    .profile-bar {
        flex-direction: column;
        align-items: stretch;
        gap: 10px;
        padding: 10px;
    }
    .profile-info {
        gap: 8px 12px;
        flex-direction: row;
        align-items: flex-start;
        justify-content: flex-start;
        width: 100%;
        flex-wrap: wrap;
    }
    .profile-item {
        width: 100%;
        font-size: 18px;
        flex: 1 1 100%;
        min-width: 0;
        white-space: normal;
        word-break: break-word;
        overflow-wrap: anywhere;
    }
    .profile-edit-btn {
        width: 100%;
        align-self: stretch;
        padding: 9px 12px;
    }
}
</style>
</head>
<body>
<div class="page">
    <div class="page-title" id="mainTitle">검은 마녀의 탑 위치 현황</div>

    <div class="profile-bar">
        <div class="profile-info">
            <div class="profile-item"><strong>아바타 :</strong> <span id="avatarText">-</span></div>
            <div class="profile-item"><strong>탈것 :</strong> <span id="mountText">-</span></div>
        </div>
        <button type="button" class="profile-edit-btn" id="profileEditBtn">수정</button>
    </div>

    <div class="board-controls">
        <div class="tabs" id="tabs">
            <button type="button" class="tab-btn active" data-id="floor1">1층</button>
            <button type="button" class="tab-btn" data-id="floor2">2층</button>
        </div>

        <div class="refresh-bar">
            <button type="button" class="refresh-btn" id="refreshBtn">새로고침</button>
        </div>
    </div>

    <div class="board-wrap">
        <div class="image-wrap" id="imageWrap">
            <img id="bgImage" src="https://i.ibb.co/0RST75vn/1.png" alt="1층 배경">
            <div class="pin-layer" id="pinLayer"></div>
        </div>
    </div>

    <div class="section">
        <div class="section-title" id="listTitle">1층 핀 목록</div>
        <div class="pin-list" id="pinList"></div>
        <div class="status-text" id="statusText"></div>
    </div>
</div>


<div class="modal-backdrop" id="introModalBackdrop">
    <div class="modal intro-modal">
        <h3>안내</h3>
        <div class="intro-message">아바타, 탈것 확인하세요!!</div>
        <div class="intro-info-box">
            <div>아바타 : <span id="introAvatarText">-</span></div>
            <div>탈것 : <span id="introMountText">-</span></div>
        </div>
        <div class="modal-buttons">
            <button class="btn btn-blue" id="introConfirmBtn" type="button">확인</button>
        </div>
    </div>
</div>

<div class="modal-backdrop" id="createModalBackdrop">
    <div class="modal">
        <h3>핀 정보 입력</h3>
        <div class="form-row">
            <label for="pinName">이름 :</label>
            <input type="text" id="pinName" placeholder="이름을 입력해주세요">
        </div>
        <div class="form-row">
            <label for="pinWeapon">무기 :</label>
            <select id="pinWeapon">
                <option value="입력 안함">입력 안함</option>
                <option value="검과방패">검과방패</option>
                <option value="전투봉">전투봉</option>
                <option value="활">활</option>
                <option value="석궁">석궁</option>
                <option value="지팡이">지팡이</option>
                <option value="사이드">사이드</option>
                <option value="전투방패">전투방패</option>
                <option value="단검">단검</option>
                <option value="대검">대검</option>
                <option value="맨손">맨손</option>
            </select>
        </div>
        <div class="modal-buttons">
            <button class="btn btn-gray" id="cancelCreateBtn" type="button">취소</button>
            <button class="btn btn-blue" id="saveBtn" type="button">저장</button>
        </div>
    </div>
</div>

<div class="modal-backdrop" id="viewModalBackdrop">
    <div class="modal">
        <h3>핀 정보</h3>
        <div class="view-row">
            <div class="view-label">이름</div>
            <div class="view-value" id="viewName"></div>
        </div>
        <div class="view-row">
            <div class="view-label">무기</div>
            <div class="view-value" id="viewWeapon"></div>
        </div>
        <div class="view-row">
            <div class="view-label">시간</div>
            <div class="view-value" id="viewDateTime"></div>
        </div>
        <div class="modal-buttons">
            <button class="btn btn-red" id="deleteBtn" type="button">삭제</button>
            <button class="btn btn-gray" id="closeViewBtn" type="button">닫기</button>
        </div>
    </div>
</div>
